add_subdirectory(Z80)
add_subdirectory(memu)
if("${TARGET}" STREQUAL "Pico")
  add_subdirectory(fatfs)
elseif("${TARGET}" STREQUAL "Circle")
  add_subdirectory(fatfs)
  add_subdirectory(circle)
endif()
